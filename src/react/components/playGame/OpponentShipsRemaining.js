//this is an optional component to add in if we have extra time.
//it would display, in a box, a graphical representation of
//the status of each of the enemy ships (how many sections burning, etc)
//and would update each time a hit was made.
//the box would be displayed on the playGame page.

import React from "react";

class OpponentShipsRemaining extends React.Component {
  render() {
    return <p>Opponent ships here</p>;
  }
}

export default OpponentShipsRemaining;
